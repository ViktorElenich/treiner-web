# CLAUDE.md — Проект treiner-web

## Владелец

- **Имя:** Еленич Виктор Сергеевич
- **ИНН:** 472702903482
- **Адрес:** Ленинградская область, Лодейнопольский район, д. Коростелево, дом 16
- **Телефон:** +7 (981) 975-06-56
- **Email:** velenic@mail.ru
- **Telegram:** @ViktorElenich

## Описание проекта

Премиальный одностраничный сайт персонального фитнес-тренера Виктора Еленича с воронкой продаж. Тёмная тема (чёрный + золотой) с автоматическим переключением на светлую тему через `prefers-color-scheme`. Уровень визуала — Awwwards-quality с GSAP-анимациями.

**GitHub:** https://github.com/ViktorElenich/treiner-web
**Деплой (Vercel):** https://viktor-treiner.vercel.app/

**Направления тренера (акцент на персональные тренировки, не на боевые искусства):**
- Тайский бокс для девушек (группа до 6 человек) — 2 200 ₽/час
- Персональные тренировки (один на один, хит) — 3 500 ₽/час
- Функциональный тренинг (группа до 8 человек) — 1 600 ₽/час

**Онлайн-программы** (кнопки «Выбрать» ведут на бота @viktortreiner_bot с deep-link для оплаты):
- СТАРТ — 2 000 ₽/мес
- ПРОГРЕСС (хит) — 3 500 ₽/мес
- РЕЗУЛЬТАТ — 6 000 ₽/мес

## Технологический стек

| Технология | Назначение | Подключение |
|---|---|---|
| HTML5 + CSS3 + Vanilla JS | Основа | — |
| GSAP + ScrollTrigger | Анимации при скролле, timeline, параллакс | CDN (jsDelivr) |
| Lenis | Smooth scroll ("масляный" скролл) | CDN |
| Swiper | 3D-карусель отзывов, touch-свайпы | CDN |
| SplitType | Разбивка текста на буквы/слова для анимаций | CDN |
| Google Fonts | Bebas Neue, Playfair Display, Montserrat | CDN |

Все библиотеки через CDN. Итого ~80KB gzipped. Без фреймворков.

## Структура файлов

```
treiner-web/
├── .gitignore              # .DS_Store, .claude/, *.log, node_modules/
├── CLAUDE.md               # Этот файл
├── README.md               # Описание для GitHub
├── index.html              # Главная страница (12 секций, ~940 строк)
├── sitemap.xml             # Карта сайта (3 URL)
├── robots.txt              # Индексация + ссылка на sitemap
├── site.webmanifest        # PWA-манифест
├── styles/
│   └── main.css            # Единый CSS (~2430 строк)
├── scripts/
│   └── main.js             # Единый JS (~970 строк)
├── assets/
│   ├── favicon.svg         # SVG-фавикон
│   ├── img/
│   │   ├── hero.jpg        # Фото тренера для Hero-секции (140KB)
│   │   ├── about.jpg       # Фото тренера с клиентом для About-секции (145KB)
│   │   └── og.jpg          # OG-изображение для соцсетей (1200x630, ~400KB)
│   └── photo-prompts.md    # Промпты для Nano Banana 2 (не в git)
├── pages/
│   ├── offer.html          # Договор публичной оферты (14 разделов)
│   └── privacy.html        # Политика обработки ПДн, 152-ФЗ (14 разделов)
└── .claude/
    ├── launch.json         # Dev-сервер (имя: fitness-site, порт 8080)
    └── settings.local.json # Локальные настройки
```

## Секции сайта (порядок воронки)

1. **Nav** — glassmorphism навигация, скрытие при скролле вниз (порог 15px), показ при скролле вверх, бургер-меню на мобайле, hover-подчёркивание золотой линией
2. **Hero** — "ТВОЁ ТЕЛО — ТВОЯ СИЛА", анимированный градиент, статистика (13+ лет, 200+ клиентов, 3 направления), две CTA-кнопки
3. **Pain** — 6 карточек "болей" клиентов с номерами и hover-эффектами
4. **About** — 2 колонки, жёлтый блок "13" лет опыта, 5 регалий тренера
5. **Services** — 3 карточки услуг, featured-карточка (персональные, 3500₽) с золотым фоном
6. **Online** — 3 тарифа онлайн-программ с переключателем "Дома/В зале", "С минимальным инвентарём"
7. **Process** — 4 шага (консультация → программа → тренировки → результат)
8. **Lead** — форма захвата "Запишись на разбор ситуации" + 4 преимущества
9. **Results** — Swiper-карусель отзывов + полоса статистики (200+, 8кг, 92%, 13 лет)
10. **FAQ** — 6 вопросов, аккордеон с GSAP-анимацией
11. **Consultation** — финальная форма (имя, телефон, направление, цель)
12. **Footer** — "Виктор Еленич — персональный тренер...", телефон, email, Telegram, юридические ссылки, "© 2026 Виктор Еленич"

## Контактные данные на сайте

- Кнопки «Выбрать» в тарифах ведут на бота `https://t.me/viktortreiner_bot?start=tariff_{id}` для оплаты
- Плавающая кнопка Telegram и footer ведут на `https://t.me/ViktorElenich` для связи с тренером
- Локация в footer: Санкт-Петербург, метро Горьковская
- Телефон и email в footer. Телефон в JSON-LD schema.

## Дизайн-система

### Цвета (CSS-переменные)

```css
--black: #0a0a0a;        /* Основной фон */
--dark: #111111;          /* Вторичный фон */
--surface: #1a1a1a;       /* Поверхности */
--card: #161616;          /* Карточки */
--border: #222222;        /* Границы */
--accent: #C8A96E;        /* Золотой акцент */
--accent-light: #e8c98e;  /* Светлый золотой */
--accent-dark: #A8894E;   /* Тёмный золотой */
--white: #f5f0eb;         /* Тёплый белый */
--muted: #888888;         /* Приглушённый текст */
```

### Шрифты

- **Bebas Neue** (`--font-heading`) — заголовки секций, цены
- **Playfair Display** (`--font-accent`) — акцентные слова (курсив)
- **Montserrat** (`--font-body`) — основной текст, кнопки, навигация

## CSS-архитектура (порядок в main.css)

1. **Reset / Normalize** (строки 1-9)
2. **Custom Properties** `:root` (строки 12-35)
3. **Base & Typography** — body, headings (строки 37+)
4. **Utilities** — `.container`, `.btn--primary`, `.btn--ghost`, `.btn--magnetic`, `.sr-only`
5. **Components** — `.card`, `.badge`, `.form-group`, `.accordion`, `.swiper` overrides
6. **Секции** — Nav → Hero → Pain → About → Services → Online → Process → Lead → Results → FAQ → Consultation → Footer
7. **Overlay элементы** — Floating CTA, Preloader, Cookie Banner, Exit Popup, Custom Cursor
8. **@keyframes** — pulse, gradient-shift, spin
9. **Media queries**:
   - `max-width: 1024px` — бургер-меню, 2 колонки
   - `max-width: 768px` — 1 колонка, уменьшение типографики
   - `max-width: 480px` — компрессия отступов, full-width кнопки
   - `prefers-reduced-motion: reduce` — отключение анимаций
   - `prefers-color-scheme: light` — светлая тема (~180 строк overrides)

## JavaScript-архитектура (main.js)

Единый IIFE с модулями:

- **CONFIG** — константы (navHeight, revealThreshold, counterDuration и т.д.)
- **Lenis** — smooth scroll, интеграция с GSAP ScrollTrigger
- **GSAP ScrollTrigger** — reveal-анимации всех секций, stagger-карточки, параллакс
- **SplitType** — побуквенные анимации заголовков
- **Nav** — скрытие/показ при скролле (порог: вниз >5px, вверх >15px)
- **Бургер-меню** — GSAP timeline, stagger fade-in пунктов
- **FAQ аккордеон** — GSAP анимация height + rotate иконки
- **Swiper** — карусель отзывов (autoHeight: false, equal heights)
- **Формы** — валидация + success-сообщение
- **Counter animation** — count-up для статистики
- **Scroll progress bar** — золотая полоска сверху
- **Прелоадер** — GSAP timeline
- **Floating CTA** — скрытие когда hero/финальная форма видны
- **Магнитные кнопки** — GSAP quickTo (сила 0.15 для hero, 0.3 для остальных)
- **Кастомный курсор** — GSAP lerp (отключён на тач-устройствах), cursor: auto на body, cursor: pointer на кнопках
- **Exit-intent popup** — 1 раз за сессию
- **Cookie consent** — GSAP slide up
- **Online toggle** — переключатель "Дома/В зале" для тарифов

## Светлая тема (prefers-color-scheme: light)

Автоматическое переключение на основе системных настроек ОС. Overrides в `@media (prefers-color-scheme: light)` блоке main.css:

- Фон: `#f5f0eb` (тёплый бежевый) вместо `#0a0a0a`
- Текст: `#1a1a1a` вместо `#f5f0eb`
- Карточки: `#fff` с `box-shadow` вместо `#161616`
- Featured-карточки (золотой фон): белый текст для контраста
- Nav: светлый градиент `rgba(245, 240, 235, 0.9)`
- Кастомный курсор скрыт в светлой теме

## SEO

- `<title>` с ключевыми словами
- `<meta name="description">` и `<meta name="keywords">`
- `<link rel="canonical">` → `https://viktor-treiner.vercel.app/`
- **Open Graph**: title, description, image, type, url, locale, site_name
- **Twitter Card**: summary_large_image
- **JSON-LD** (3 схемы):
  - `LocalBusiness` — 3 услуги, цены, телефон, адрес, часы работы
  - `Person` — тренер, контакты, специализация
  - `FAQPage` — структурированные вопросы/ответы из секции FAQ
- `robots.txt` — разрешение индексации, Yandex crawl-delay, ссылка на sitemap
- `sitemap.xml` — 3 URL (главная, оферта, политика), lastmod 2026-03-27
- Семантический HTML5: `<nav>`, `<section>`, `<header>`, `<footer>`, `<form>`

## Юридические документы

### Договор-оферта (pages/offer.html) — 14 разделов:
1. Общие положения (акцепт через форму/оплату/Telegram)
2. Предмет договора (не медицинская деятельность)
3. Стоимость и оплата (срок пакета 30 дней)
4. **Состояние здоровья и принятие рисков** (клиент подтверждает отсутствие противопоказаний)
5. Права и обязанности сторон
6. Отмена и перенос (24 часа, no-show = проведена)
7. **Онлайн-программы** (нет контроля техники дистанционно, не гарантирует результат)
8. **Интеллектуальная собственность** (штраф 50 000₽ за распространение)
9. **Ограничение ответственности** (7 оснований, совокупная ответственность = сумма оплаты)
10. Возврат (по ст. 32 ЗоЗПП, минус фактические расходы)
11. Возрастные ограничения (18+, 14-18 с согласия родителей)
12. Форс-мажор (включая блокировку Telegram)
13. Разрешение споров (претензионный порядок 10 дней)
14. Прочие условия

### Политика конфиденциальности (pages/privacy.html) — 14 разделов, 152-ФЗ:
1. Общие положения (ссылки на 152-ФЗ, 149-ФЗ, ПП 1119)
2. Сведения об операторе (все данные Виктора заполнены)
3. Категории данных (таблица: посетители, лиды, клиенты, подписчики)
4. Цели обработки
5. Правовые основания
6. Порядок обработки
7. Сроки хранения (таблица: лиды 1 год, клиенты +3 года, бухгалтерия 5 лет)
8. Третьи лица (таблица: Telegram, Google Fonts, jsDelivr)
9. Трансграничная передача (Telegram)
10. Cookie (таблица: технические, функциональные, аналитические)
11. Меры безопасности
12. Права субъекта (отзыв согласия, жалоба в Роскомнадзор)
13. Уничтожение данных
14. Изменения

## История исправленных багов

- **Кнопки Hero перекрывались**: `isolation: isolate` + z-index, магнитный эффект 0.3 → 0.15
- **Онлайн-карточки невидимы**: убран opacity из CSS transition, переход на `gsap.fromTo()`
- **Неравная высота отзывов**: `.swiper-wrapper { align-items: stretch }`
- **Статистика Hero/Results съезжала**: `flex-wrap: wrap; align-items: baseline`
- **Тексты невидимы в light theme**: добавлены overrides для featured cards, service features, about credentials, `<p>`, nav gradient
- **Цена персональных**: 3000₽ → 3500₽
- **Service-link hover прыжок**: `letter-spacing` → плавный `padding-left: 6px`
- **"Без инвентаря"** → "С минимальным инвентарём"
- **Курсор исчезал**: `cursor: none` → `cursor: auto`/`pointer`
- **Хедер скачет**: порог скролла вниз >5px, вверх >15px
- **Nav ссылки без hover**: добавлен ::after с золотой линией
- **Footer-contact не читался в light theme**: добавлены overrides `.footer .footer-contact` и `.footer .footer-contact strong` с явными цветами
- **"район уточняется на консультации"** → "метро Горьковская"
- **OG-теги были относительные**: `assets/img/og.jpg` → абсолютные URL + добавлены og:image:width/height

## Плейсхолдеры для замены

| Плейсхолдер | Где | Что заменить |
|---|---|---|
| `webhookUrl: ''` | `scripts/main.js` (CONFIG, строка 13) | URL webhook для отправки форм |
| `webhookSecret: ''` | `scripts/main.js` (CONFIG, строка 15) | Секрет для подписи webhook-запросов |

## Фотографии

Промпты для обработки в Nano Banana 2 сохранены в `assets/photo-prompts.md` (не в git).

1. **Hero** — `assets/img/hero.jpg` (140KB, 896×1089) — готово
2. **About** — `assets/img/about.jpg` (145KB, 896×1195) — готово
3. **OG** — `assets/img/og.jpg` (~400KB, 1200×630) — готово, превью работает в Telegram/соцсетях

Акцент на персональные тренировки, не на боевые искусства.

## Dev-сервер

```bash
python3 -m http.server 8080
```

Конфигурация в `.claude/launch.json` (имя: `fitness-site`).

## Что сделано

- [x] Лендинг с 12 секциями воронки продаж (HTML + CSS + JS)
- [x] Тёмная/светлая тема (`prefers-color-scheme`)
- [x] GSAP-анимации: reveal, parallax, SplitType, магнитные кнопки, кастомный курсор
- [x] Smooth scroll (Lenis), Swiper-карусель отзывов
- [x] Прелоадер, exit-intent popup, cookie consent, floating CTA
- [x] Формы с валидацией (Lead + Consultation)
- [x] Юридические документы: оферта (14 разделов) + политика конфиденциальности 152-ФЗ (14 разделов)
- [x] SEO: JSON-LD (LocalBusiness + Person + FAQPage), Open Graph, Twitter Card, sitemap.xml
- [x] Деплой на Vercel — https://viktor-treiner.vercel.app/
- [x] Hero-фото тренера (`assets/img/hero.jpg`)
- [x] About-фото (`assets/img/about.jpg`)
- [x] OG-изображение (`assets/img/og.jpg`, 1200×630) — превью в соцсетях работает
- [x] OG-теги с абсолютными URL + og:image:width/height
- [x] Адаптив: 4 брейкпоинта (1024, 768, 480) + reduced-motion
- [x] PWA-манифест (`site.webmanifest`)
- [x] Кнопки «Выбрать» в тарифах → deep-link на бота для оплаты
- [x] Footer: локация «метро Горьковская», исправлены цвета в light theme

## Связанный проект: Telegram-бот

**Репозиторий:** `/Users/viktor/treiner-bot`
**Стек:** Python 3, aiogram 3, ЮKassa, SQLite
**Бот:** @viktortreiner_bot

Бот для автоматизации подписок на онлайн-программы:
- Этап 1 (готов): /start, меню тарифов, консультация, информация о тренере, deep-links с сайта
- Этап 2 (готов): оплата через ЮKassa (тестовый режим), автодоступ к группам тарифов
- Этап 3 (готов): модерация чатов — фильтр мата по словарю, 3 предупреждения → бан

### Telegram-группы

| Группа | ID | Доступ |
|--------|-----|--------|
| Общий чат | `-1003786144006` | Все (после оплаты любого тарифа) |
| СТАРТ | `-1003602025479` | Подписчики тарифа СТАРТ |
| ПРОГРЕСС | `-1003756564153` | Подписчики тарифа ПРОГРЕСС |
| РЕЗУЛЬТАТ | `-1003846945839` | Подписчики тарифа РЕЗУЛЬТАТ |

### Модерация чатов

- Метод: список запрещённых слов с нормализацией (обход маскировки: `х*й`, `п1зд@`, `б.л.я.т.ь`)
- Наказания: 1-е → предупреждение, 2-е → мут 1 час, 3-е → бан
- Админ (тренер) не проверяется
- Уведомления тренеру о каждом нарушении
- Команды: `/warnings` (проверить), `/reset_warnings` (сбросить) — только для админа
- Файлы: `bot/data/bad_words.py`, `bot/handlers/moderation.py`

## Что осталось сделать

- [ ] Интеграция форм сайта с ботом (webhook → заявки тренеру в Telegram)
- [ ] Деплой бота на Render для работы 24/7
- [ ] Переход на боевые ключи ЮKassa (сейчас тестовый режим)
- [ ] Этап 4 бота: управление подписками, напоминания об истечении, авто-кик из групп
- [ ] Яндекс.Метрика / Google Analytics
- [ ] Страница 404
- [ ] Реальные отзывы с фотографиями клиентов
- [ ] Уведомление в Роскомнадзор об обработке персональных данных
